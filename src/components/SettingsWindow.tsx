import React from 'react';

const SettingsWindow: React.FC = () => {
    return (
        <div className="settings-window">
            <h2>Cat Settings (Identity & Customization)</h2>
            {/* TODO: 털색, 눈색, 모드 설정 UI */}
        </div>
    );
};

export default SettingsWindow;
